# .env
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/flashcards_app?schema=public"

# For Puppeteer in Docker, sometimes specific flags are needed
# or disabling sandbox, depending on the base image and environment.
# Example:
# PUPPETEER_EXECUTABLE_PATH=/usr/bin/google-chrome-stable
